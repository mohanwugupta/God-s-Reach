TASK 1: Find parameters from the library that regex extraction MISSED.

You are validating parameter extraction from a scientific paper.

INPUTS
- PARAMETER LIBRARY (eligible only): {current_schema}
- ALREADY EXTRACTED (do NOT repeat any of these): {already_extracted}
- PAPER CONTEXT: {context}

REQUIREMENTS
1) Only report parameters present in the PARAMETER LIBRARY and NOT listed in ALREADY EXTRACTED.
2) Only include parameters that have explicit, measurable values in the text.
3) Each item must include a verbatim evidence quote ≥ 20 characters, plus a location (section/page/line if available).
4) Hard cap: 15 items. If more are found, keep the earliest mentions.
5) Confidence calibration:
   - explicit numeric/category stated plainly → 0.90–0.99
   - implied but precise (e.g., “a 1 kHz… sampled”) → 0.70–0.85
   - uncertain edge cases → 0.50–0.65
6) JSON ONLY. No preface, no notes, no thoughts.

OUTPUT (strict JSON):
{
  "missed_parameters": [
    {
      "parameter_name": "snake_case_from_library",
      "value": 123,
      "units": "ms",
      "confidence": 0.93,
      "evidence": "verbatim quote of ≥ 20 characters…",
      "evidence_location": {"section": "Methods", "page": 4, "line_hint": "p4¶2"}
    }
  ]
}

VALIDATION
- Return {} if nothing is found: {"missed_parameters": []}
- Output must match this JSON Schema (informal):
  missed_parameters: array(maxItems=15) of {
    parameter_name: string; value: string|number|boolean|array;
    units?: string; confidence: number in [0.5, 0.99];
    evidence: string(len≥20); evidence_location: object{section?:string,page?:number,line_hint?:string}
  }
